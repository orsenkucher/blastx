# blastx
Helix editor theme

## Setup
Put into `~/.config/helix/themes/blastx.toml`.

## Config
`~/.config/helix/config.toml`
```toml
theme = "blastx"

[editor]
true-color = true
auto-pairs = true
```
